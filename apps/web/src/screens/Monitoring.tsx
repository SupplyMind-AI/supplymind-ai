import {useEffect,useState} from "react";import {api} from "../api";import {Header,Card} from "../components";export default function Monitoring(){const[d,setD]=useState<any>(null);useEffect(()=>{api("/monitoring").then(setD)},[]);return <><Header title="Model Monitoring" subtitle="Champion performance snapshots and drift signals."/><Card><pre>{JSON.stringify(d,null,2)}</pre></Card></>}
