import{useEffect,useState}from"react";import{api}from"../api";import{Header,Card,RiskBadge}from"../components";export default function Alerts(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api('/alerts').then(setD)},[]);return <><Header title="Alerts" subtitle="High-priority model predictions and external disruption signals."/><div className="alert-list">{d.map(x=><Card key={x.id}><div className="alert-item"><span className="alert-icon">!</span><div><b>{x.title}</b><p>{x.detail}</p></div><RiskBadge level={x.severity}/></div></Card>)}</div></>}
