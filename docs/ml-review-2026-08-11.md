# ML Review — 11 August 2026

## Fixed

- Restored the real raw SynDelay contract: 41 source columns.
- Canonicalization now creates `delivery_outcome` and binary `is_delayed` from `label`.
- Target consistency is validated after canonicalization.
- Chronological split remains 70/15/15 on `order_date`.
- Feature engineering state is now persisted inside the sklearn pipeline.
- Frequency maps are learned only from training data and reused for validation,
  test, retraining, and future FastAPI inference.
- One-hot encoding remains inside the persisted preprocessing stage.
- HistGradientBoosting now receives dense encoded input.
- Feature-selection diagnostics use the training period rather than the full
  future dataset.
- Domain prediction schema now accepts raw business inputs rather than asking
  API callers to provide engineered frequencies/calendar features.
- Removed stale notebook execution output from the previous schema error.
- Added/updated ML tests; current result: 9 passed.

## Verified against the uploaded dataset

- Raw CSV shape: 155,488 rows x 41 columns.
- Source target column: `label`.
- Source class counts in the uploaded file:
  - class 0: 36,724
  - class 1: 29,055
  - class 2: 89,709
- Canonical dataset adds two columns, producing 43 columns.
- `is_delayed` is derived as `label == 2`.

## Next command sequence

```bash
pip install -e ".[dev]"
pytest -q tests/ml
python scripts/run_eda.py
python scripts/train_models.py
```

Run the notebooks in numerical order when you want the documented Ironhack
analysis narrative. Use the scripts for reproducible end-to-end artifact generation.
